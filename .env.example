# .env.example - Copia este archivo a .env para desarrollo local

# Intervalo de chequeo (formato cron)
# */5 * * * * = cada 5 minutos
# */10 * * * * = cada 10 minutos
SETTINGS_CHECK_INTERVAL="*/5 * * * *"

# Número de reintentos antes de marcar como down
SETTINGS_RETRIES=3

# Delay entre reintentos (en milisegundos)
SETTINGS_RETRY_DELAY=1000
